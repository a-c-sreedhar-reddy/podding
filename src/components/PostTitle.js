import React from "react"
export default props => (
  <div
    style={{
      fontSize: 30,
      paddingLeft: 0,
      marginBottom:1,
      fontWeight: "bold",
      color: "lightcoral",
    }}
  >
    {props.title}
  </div>
)
